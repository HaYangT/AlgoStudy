import java.util.*;

public class Solution {
    /**
     * 에베레스트산 휴게소 문제
     *
     * @param N       봉우리(정점)의 수 (1 <= N <= 100,000)
     * @param H       산의 높이 (1 <= H <= 1,000,000,000)
     * @param edges   간선 정보 (N-1개), 각 원소는 {u, v, d}
     *                u에서 v로 가는 길의 길이가 d
     * @param queries 쿼리 배열, 각 원소는 높이 h
     * @return 각 쿼리에 대해 높이가 h 이상인 봉우리의 개수
     *
     */
    public int[] solution(int N, long H, int[][] edges, long[] queries) {
        // 여기에 풀이를 작성하세요
        return new int[queries.length];
    }
}
